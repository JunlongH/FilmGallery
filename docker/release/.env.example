# FilmGallery NAS Server Configuration
# Copy this file to .env and modify as needed

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# Data storage path (where film.db will be stored)
# Use absolute path or relative to docker-compose.yml
DATA_PATH=./data

# Uploads storage path (where image files will be stored)
# Use absolute path or relative to docker-compose.yml
UPLOADS_PATH=./uploads

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================

# Docker image version (default: latest)
# Recommended: Use specific version for production stability
# Example: IMAGE_VERSION=1.8.0
IMAGE_VERSION=latest

# Server port (default: 4000)
PORT=4000

# Timezone (default: Asia/Shanghai)
TZ=Asia/Shanghai

# Enable database write-through mode for cloud sync (OneDrive/Dropbox)
# 0 = WAL mode (faster, recommended for local storage)
# 1 = Write-through mode (slower, but more compatible with cloud sync)
DB_WRITE_THROUGH=0

# =============================================================================
# ADVANCED SETTINGS (usually no need to change)
# =============================================================================

# Server mode (do not change unless you know what you're doing)
# SERVER_MODE=nas

# Node environment
# NODE_ENV=production
