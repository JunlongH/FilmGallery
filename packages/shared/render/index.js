/**
 * FilmLab 渲染模块入口
 * 
 * @module render
 */

'use strict';

const { RenderCore, DEFAULT_FILM_CURVE, DEFAULT_CROP_RECT } = require('./RenderCore');

module.exports = {
  RenderCore,
  DEFAULT_FILM_CURVE,
  DEFAULT_CROP_RECT,
};
