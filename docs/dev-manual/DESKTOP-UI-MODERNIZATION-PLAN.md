# FilmGallery 桌面端 UI 现代化改造方案

> **目标**: 使用 HeroUI (NextUI v3) 打造现代、简洁、美观、动态的桌面应用界面
> 
> **优先级**: Timeline、Life Log (PhotoCalendar)、Overview、Sidebar
> 
> **暂缓**: FilmLab (复杂度极高，保持现有UI)
> 
> **设计理念**: 极简主义 × 摄影美学 × 流畅动效

---

## 📋 目录

1. [当前状态分析](#当前状态分析)
2. [技术栈选择](#技术栈选择)
3. [设计系统](#设计系统)
4. [页面改造方案](#页面改造方案)
5. [组件库迁移](#组件库迁移)
6. [实施路线图](#实施路线图)
7. [性能优化](#性能优化)
8. [风险评估与缓解策略](#风险评估与缓解策略)

---

## 当前状态分析

### 技术栈现状

```json
{
  "UI库": "纯手写CSS + 少量framer-motion",
  "图表": "recharts",
  "地图": "leaflet + react-leaflet",
  "3D": "three.js + react-globe.gl",
  "状态管理": "@tanstack/react-query",
  "路由": "react-router-dom v7",
  "样式": "原生CSS + CSS变量"
}
```

### 主要页面清单

| 页面/组件 | 当前状态 | 改造优先级 | 复杂度 | 备注 |
|---------|---------|----------|-------|------|
| **Timeline** | 基础HTML+CSS | ⭐⭐⭐⭐⭐ 最高 | 高 | Phase 1 |
| **PhotoCalendar (Life Log)** | 自定义日历 | ⭐⭐⭐⭐⭐ 最高 | 中 | Phase 1, HeroUI有Calendar组件 |
| **Sidebar导航** | 传统侧边栏 | ⭐⭐⭐⭐⭐ 最高 | 低 | Phase 1, 全局影响 |
| **Overview** | 基础网格布局 | ⭐⭐⭐⭐ 高 | 中 | Phase 2 |
| **RollDetail** | 照片网格+侧边栏 | ⭐⭐⭐ 中 | 高 | Phase 2 |
| **FilmLibrary** | 库存管理表格 | ⭐⭐⭐ 中 | 中 | Phase 3 |
| **Statistics** | recharts图表 | ⭐⭐ 中低 | 中 | Phase 3, 保留recharts |
| **Settings** | 表单页面 | ⭐⭐ 中低 | 低 | Phase 3 |
| **MapPage** | 3D地球+2D地图 | ⭐⭐ 中低 | 高 | Phase 3, 保留地图库 |
| **FilmLab** | WebGL编辑器 | ⚠️ 暂缓 | 极高 | 功能复杂，暂不改造 |

### 当前UI问题

1. **视觉层面**
   - 缺乏统一的设计语言
   - 组件样式不一致（按钮、输入框、卡片等）
   - 色彩系统不够现代（米黄色系过时）
   - 缺少深色模式支持
   - 图标系统不统一

2. **交互层面**
   - 动画效果稀少（仅部分页面有framer-motion）
   - 加载状态不统一
   - 反馈不够及时
   - 模态框样式老旧
   - 表单交互体验一般

3. **响应式**
   - 部分组件缺少响应式支持
   - 布局在不同分辨率下表现不一致

---

## 技术栈选择

### HeroUI (NextUI v3) - 首选方案 ✅

**选择理由**:

1. **完美契合**
   - 基于 React + Tailwind CSS
   - 提供 60+ 高质量组件
   - 原生支持 Framer Motion 动画
   - 内置深色模式
   - TypeScript 完整支持

2. **摄影应用优势**
   - 美观的 Card/Image 组件
   - 强大的 Modal/Drawer 系统
   - 优雅的日历组件（关键！）
   - Timeline 组件支持
   - Gallery/Grid 布局组件

3. **性能优势**
   - 按需加载
   - Tree-shaking 友好
   - SSR/CSR 兼容
   - 无运行时开销（零 runtime CSS-in-JS）

4. **生态系统**
   - 活跃的社区
   - 完善的文档
   - 定期更新
   - 兼容 React 18+

### 依赖安装

```bash
# 进入 client 目录
cd client

# HeroUI 核心 (推荐按需安装，减少包体积)
npm install @heroui/react framer-motion

# 或者按需安装单个组件 (推荐)
npm install @heroui/button @heroui/card @heroui/tabs @heroui/modal @heroui/accordion @heroui/calendar @heroui/chip @heroui/avatar @heroui/image @heroui/input @heroui/select @heroui/slider @heroui/switch @heroui/badge @heroui/progress @heroui/spinner @heroui/tooltip @heroui/dropdown @heroui/listbox @heroui/drawer

# Tailwind CSS (已通过 craco 使用，需额外配置)
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

# 图标库 (轻量级，与 HeroUI 搭配)
npm install lucide-react

# 国际化日期库 (HeroUI Calendar 依赖)
npm install @internationalized/date
```

### CRACO 配置更新

由于项目使用 CRACO，需要更新配置以支持 Tailwind CSS：

```javascript
// client/craco.config.js
const path = require('path');

module.exports = {
  webpack: {
    alias: {
      '@filmgallery/shared': path.resolve(__dirname, '../packages/shared'),
    }
  },
  style: {
    postcss: {
      mode: 'file', // 使用 postcss.config.js
    },
  },
};
```

```javascript
// client/postcss.config.js (新建)
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

### 配置文件

#### tailwind.config.js (client/tailwind.config.js)

```javascript
const { heroui } = require("@heroui/react");

/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
    "./public/index.html",
    // 重要：包含 HeroUI 组件
    "./node_modules/@heroui/theme/dist/**/*.{js,ts,jsx,tsx}"
  ],
  theme: {
    extend: {
      colors: {
        // FilmGallery 品牌色
        film: {
          50: '#fef7f0',
          100: '#fceee0',
          200: '#f8ddc1',
          300: '#f5cba2',
          400: '#f1ba83',
          500: '#eea964',
          600: '#be8750',
          700: '#8f653c',
          800: '#5f4428',
          900: '#302214',
        },
      },
      // 扩展动画
      animation: {
        'fade-in': 'fadeIn 0.3s ease-out',
        'slide-up': 'slideUp 0.3s ease-out',
        'scale-in': 'scaleIn 0.2s ease-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' },
        },
        slideUp: {
          '0%': { opacity: '0', transform: 'translateY(10px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  darkMode: "class",
  plugins: [heroui({
    // 添加前缀避免与现有样式冲突
    prefix: "heroui",
    themes: {
      light: {
        colors: {
          background: "#ffffff",
          foreground: "#1a1a1a",
          primary: {
            50: "#fef7f0",
            100: "#fceee0",
            200: "#f8ddc1",
            300: "#f5cba2",
            400: "#f1ba83",
            500: "#eea964",
            600: "#be8750",
            700: "#8f653c",
            800: "#5f4428",
            900: "#302214",
            DEFAULT: "#eea964",
            foreground: "#ffffff",
          },
          secondary: {
            DEFAULT: "#5f4428",
            foreground: "#ffffff",
          },
          success: {
            DEFAULT: "#2f7d32",
            foreground: "#ffffff",
          },
          focus: "#eea964",
        },
      },
      dark: {
        colors: {
          background: "#0a0a0a",
          foreground: "#ededed",
          primary: {
            50: "#302214",
            100: "#5f4428",
            200: "#8f653c",
            300: "#be8750",
            400: "#eea964",
            500: "#f1ba83",
            600: "#f5cba2",
            700: "#f8ddc1",
            800: "#fceee0",
            900: "#fef7f0",
            DEFAULT: "#f5cba2",
            foreground: "#302214",
          },
          secondary: {
            DEFAULT: "#8f653c",
            foreground: "#ffffff",
          },
          success: {
            DEFAULT: "#4caf50",
            foreground: "#ffffff",
          },
          focus: "#f5cba2",
        },
      },
    },
  })],
};
```

#### 入口样式文件更新

```css
/* client/src/index.css (在文件顶部添加) */
@tailwind base;
@tailwind components;
@tailwind utilities;

/* 保留现有的 CSS 变量以实现渐进式迁移 */
```

---

## 设计系统

### 色彩方案

#### Light Mode (主题)
```css
--primary: #eea964 (暖金色 - 胶片色调)
--secondary: #5f4428 (深褐色)
--background: #ffffff
--foreground: #1a1a1a
--card: #fafafa
--border: #e5e5e5
--accent: #2f7d32 (森林绿 - CTA)
```

#### Dark Mode (新增)
```css
--primary: #f5cba2 (浅金色)
--secondary: #8f653c
--background: #0a0a0a
--foreground: #ededed
--card: #1a1a1a
--border: #2a2a2a
--accent: #4caf50
```

### 间距系统 (8px Grid)
```
xs: 4px
sm: 8px
md: 16px
lg: 24px
xl: 32px
2xl: 48px
3xl: 64px
```

### 圆角系统
```
none: 0
sm: 4px
md: 8px
lg: 12px
xl: 16px
2xl: 24px
full: 9999px
```

### 阴影系统
```
sm: 0 1px 3px rgba(0,0,0,0.12)
md: 0 4px 6px rgba(0,0,0,0.1)
lg: 0 10px 15px rgba(0,0,0,0.1)
xl: 0 20px 25px rgba(0,0,0,0.1)
2xl: 0 25px 50px rgba(0,0,0,0.15)
```

### 动画曲线
```
ease-out: cubic-bezier(0.33, 1, 0.68, 1)
ease-in: cubic-bezier(0.32, 0, 0.67, 0)
ease-in-out: cubic-bezier(0.65, 0, 0.35, 1)
spring: { type: "spring", stiffness: 300, damping: 25 }
```

---

## 页面改造方案

### 🔥 Phase 1: Timeline 视图改造

**当前状态**: 基础的年份/月份/日期展示，使用原生表格布局

**改造目标**: 打造媲美 Apple Photos 的时间轴体验

#### 设计稿 (描述)

```
┌─────────────────────────────────────────────────────────────┐
│  Timeline                                      [Year] [2026] │ ← Breadcrumb + Chip
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  📅 2026                            [Collapse] [^]  │   │ ← Accordion Header (HeroUI)
│  ├─────────────────────────────────────────────────────┤   │
│  │  ┌──────────┬──────────┬──────────┬──────────┐     │   │
│  │  │   JAN    │   FEB    │   MAR    │   APR    │     │   │ ← Month Cards (Hover: Scale)
│  │  │  ┌────┐  │  ┌────┐  │  ┌────┐  │  ┌────┐  │     │   │
│  │  │  │ 🎞️ │  │  │ 🎞️ │  │  │ 🎞️ │  │  │    │  │     │   │
│  │  │  └────┘  │  └────┘  │  └────┘  │  └────┘  │     │   │
│  │  │  15 rolls│  12 rolls│  8 rolls │  0 rolls │     │   │
│  │  └──────────┴──────────┴──────────┴──────────┘     │   │
│  │                                                      │   │
│  │  [Click to expand →]                                │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  📅 2025                            [Expand] [v]    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  📅 2024                            [Expand] [v]    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                               │
└─────────────────────────────────────────────────────────────┘

Month Detail View (点击月份后):
┌─────────────────────────────────────────────────────────────┐
│  2026 > January                                [Back] [←]   │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  Week 1 ─────────────────────────────────────────────────   │
│  ┌─────┬─────┬─────┬─────┬─────┬─────┬─────┐              │
│  │ Sun │ Mon │ Tue │ Wed │ Thu │ Fri │ Sat │              │
│  │  1  │  2  │  3  │  4  │  5  │  6  │  7  │              │
│  │ ● ● │  ●  │     │ ●●● │  ●  │     │ ● ● │              │ ← Dots = Rolls
│  └─────┴─────┴─────┴─────┴─────┴─────┴─────┘              │
│                                                               │
│  Week 2 ─────────────────────────────────────────────────   │
│  [Similar layout...]                                         │
│                                                               │
│  Rolls in this period ──────────────────────────────────    │
│  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐    │
│  │  [Cover Img]  │ │  [Cover Img]  │ │  [Cover Img]  │    │ ← Card Carousel
│  │  Kodak Gold   │ │  Portra 400   │ │  HP5 Plus     │    │
│  │  Canon AE-1   │ │  Pentax K1000 │ │  Leica M6     │    │
│  │  Jan 3, 2026  │ │  Jan 5, 2026  │ │  Jan 8, 2026  │    │
│  └───────────────┘ └───────────────┘ └───────────────┘    │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

#### HeroUI 组件映射

1. **Accordion** - 年份折叠面板
   ```jsx
   <Accordion variant="splitted">
     <AccordionItem key="2026" title="📅 2026" subtitle="45 rolls shot">
       {/* 月份网格 */}
     </AccordionItem>
   </Accordion>
   ```

2. **Card** - 月份卡片
   ```jsx
   <Card isPressable onPress={() => selectMonth('2026-01')}>
     <CardBody>
       <div className="text-4xl mb-2">🎞️</div>
       <h3>JANUARY</h3>
       <p className="text-small text-default-500">15 rolls</p>
     </CardBody>
   </Card>
   ```

3. **Chip** - 统计标签
   ```jsx
   <Chip color="primary" variant="flat">
     {rollsCount} rolls
   </Chip>
   ```

4. **Progress** - 月度进度
   ```jsx
   <Progress 
     value={(rollsCount / maxRolls) * 100}
     color="warning"
     className="w-full"
   />
   ```

#### 动画设计

```jsx
// 月份卡片进入动画
const monthVariants = {
  hidden: { opacity: 0, scale: 0.8 },
  visible: (i) => ({
    opacity: 1,
    scale: 1,
    transition: {
      delay: i * 0.05,
      duration: 0.4,
      ease: "easeOut"
    }
  })
};

// 年份展开动画
const yearVariants = {
  collapsed: { height: 60 },
  expanded: { 
    height: "auto",
    transition: {
      duration: 0.5,
      ease: [0.04, 0.62, 0.23, 0.98]
    }
  }
};
```

#### 代码结构

```
components/Timeline/
├── TimelineView.jsx (主容器)
├── YearSection.jsx (年份手风琴)
├── MonthGrid.jsx (月份网格)
├── MonthCard.jsx (月份卡片)
├── WeekCalendar.jsx (周视图)
├── RollCarousel.jsx (胶卷轮播)
└── timelineUtils.js (日期处理工具)
```

---

### 🔥 Phase 1: Sidebar 现代化改造

**当前状态**: 传统的 `<ul><li>` 列表导航，硬编码 SVG 图标

**改造目标**: 现代化、响应式、带动画的侧边栏

#### 设计稿 (描述)

```
┌─────────────────────────────────────────┐
│  ┌───────────────────────────────────┐  │
│  │  🎞️ Film Gallery                  │  │ ← Logo + Title
│  └───────────────────────────────────┘  │
│                                          │
│  ─────── LIBRARY ──────────────────────  │ ← Section Divider
│                                          │
│  ┌───────────────────────────────────┐  │
│  │ 🏠 Overview                     ●  │  │ ← Active Indicator
│  ├───────────────────────────────────┤  │
│  │ 📷 Rolls                          │  │
│  ├───────────────────────────────────┤  │
│  │ 🎞️ Films                          │  │
│  ├───────────────────────────────────┤  │
│  │ 📅 Calendar                       │  │
│  ├───────────────────────────────────┤  │
│  │ 🗺️ Map                            │  │
│  └───────────────────────────────────┘  │
│                                          │
│  ─────── COLLECTIONS ──────────────────  │
│                                          │
│  ┌───────────────────────────────────┐  │
│  │ ❤️ Favorites                      │  │
│  ├───────────────────────────────────┤  │
│  │ 🏷️ Themes                    [12] │  │ ← Badge Count
│  │    └─ Streets · Tokyo · Nature    │  │ ← Collapsible Tags
│  └───────────────────────────────────┘  │
│                                          │
│  ─────── TOOLS ────────────────────────  │
│                                          │
│  ┌───────────────────────────────────┐  │
│  │ 📊 Statistics                     │  │
│  ├───────────────────────────────────┤  │
│  │ ⚙️ Settings                       │  │
│  └───────────────────────────────────┘  │
│                                          │
│  ─────────────────────────────────────  │
│  v1.9.2                     [🌙 Dark]   │ ← Version + Theme Toggle
└─────────────────────────────────────────┘
```

#### HeroUI 组件映射

```jsx
// components/Sidebar/Sidebar.jsx
import { Listbox, ListboxItem, ListboxSection } from "@heroui/listbox";
import { Chip } from "@heroui/chip";
import { Divider } from "@heroui/divider";
import { Avatar } from "@heroui/avatar";
import { Switch } from "@heroui/switch";
import { Tooltip } from "@heroui/tooltip";
import { useLocation, useNavigate } from "react-router-dom";
import { Home, Camera, Film, Calendar, Map, Heart, Tag, BarChart, Settings, Moon, Sun } from "lucide-react";

export function Sidebar() {
  const location = useLocation();
  const navigate = useNavigate();
  const [isDark, setIsDark] = useState(false);

  const navItems = [
    { key: "/", icon: Home, label: "Overview", section: "library" },
    { key: "/rolls", icon: Camera, label: "Rolls", section: "library" },
    { key: "/films", icon: Film, label: "Films", section: "library" },
    { key: "/calendar", icon: Calendar, label: "Calendar", section: "library" },
    { key: "/map", icon: Map, label: "Map", section: "library" },
    { key: "/favorites", icon: Heart, label: "Favorites", section: "collections" },
    { key: "/themes", icon: Tag, label: "Themes", section: "collections" },
    { key: "/stats", icon: BarChart, label: "Statistics", section: "tools" },
    { key: "/settings", icon: Settings, label: "Settings", section: "tools" },
  ];

  return (
    <aside className="w-56 h-full bg-content1 border-r border-divider flex flex-col">
      {/* Logo */}
      <div className="p-4 flex items-center gap-3">
        <Avatar src="/icon.png" size="sm" />
        <span className="font-bold text-lg">Film Gallery</span>
      </div>
      
      <Divider />
      
      {/* Navigation */}
      <Listbox
        aria-label="Navigation"
        selectedKeys={[location.pathname]}
        onSelectionChange={(keys) => navigate([...keys][0])}
        selectionMode="single"
        className="flex-1 p-2"
      >
        <ListboxSection title="Library" showDivider>
          {navItems.filter(i => i.section === "library").map(item => (
            <ListboxItem
              key={item.key}
              startContent={<item.icon size={18} />}
            >
              {item.label}
            </ListboxItem>
          ))}
        </ListboxSection>
        
        <ListboxSection title="Collections" showDivider>
          {navItems.filter(i => i.section === "collections").map(item => (
            <ListboxItem
              key={item.key}
              startContent={<item.icon size={18} />}
              endContent={item.key === "/themes" && <Chip size="sm">12</Chip>}
            >
              {item.label}
            </ListboxItem>
          ))}
        </ListboxSection>
        
        <ListboxSection title="Tools">
          {navItems.filter(i => i.section === "tools").map(item => (
            <ListboxItem
              key={item.key}
              startContent={<item.icon size={18} />}
            >
              {item.label}
            </ListboxItem>
          ))}
        </ListboxSection>
      </Listbox>
      
      <Divider />
      
      {/* Footer */}
      <div className="p-4 flex items-center justify-between">
        <span className="text-tiny text-default-400">v1.9.2</span>
        <Tooltip content={isDark ? "Light Mode" : "Dark Mode"}>
          <Switch
            size="sm"
            isSelected={isDark}
            onValueChange={setIsDark}
            startContent={<Sun size={14} />}
            endContent={<Moon size={14} />}
          />
        </Tooltip>
      </div>
    </aside>
  );
}
```

#### 动画设计

```jsx
// Sidebar 展开/折叠动画 (可选功能)
const sidebarVariants = {
  expanded: { width: 224 },
  collapsed: { width: 64 }
};

// 导航项切换动画
const itemVariants = {
  inactive: { backgroundColor: "transparent" },
  active: { 
    backgroundColor: "var(--heroui-primary-100)",
    transition: { duration: 0.2 }
  }
};
```

---

### 🔥 Phase 1: PhotoCalendar (Life Log) 改造

**当前状态**: 自定义日历实现，基础的月/年视图切换

**改造目标**: 打造类似 Apple Photos 的 Memories 体验

#### 设计稿 (描述)

```
┌─────────────────────────────────────────────────────────────┐
│  Life Log                     [Timeline] [Calendar] Toggle  │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                JANUARY 2026              [<] [>]    │   │ ← Month Switcher
│  ├─────────────────────────────────────────────────────┤   │
│  │  Sun   Mon   Tue   Wed   Thu   Fri   Sat           │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │        1     2     3     4     5     6     7        │   │
│  │  ┌───┐┌───┐┌───┐┌───┐┌───┐┌───┐┌───┐              │   │
│  │  │   ││🖼️││🖼️││   ││🖼️││   ││🖼️│              │   │ ← Photo Thumbnails
│  │  └───┘└───┘└───┘└───┘└───┘└───┘└───┘              │   │
│  │   0ph  3ph  5ph  0ph  2ph  0ph  4ph                │   │ ← Count Badges
│  │                                                      │   │
│  │   8     9    10    11    12    13    14            │   │
│  │  ┌───┐┌───┐┌───┐┌───┐┌───┐┌───┐┌───┐              │   │
│  │  │🖼️││   ││🖼️││🖼️││   ││🖼️││   │              │   │
│  │  └───┘└───┘└───┘└───┘└───┘└───┘└───┘              │   │
│  │   1ph  0ph  6ph  3ph  0ph  8ph  0ph                │   │
│  │                                                      │   │
│  │  [... more weeks ...]                               │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                               │
│  Selected Day: January 3, 2026 (5 photos) ─────────────    │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐ │ │
│  │  │  Photo  │  │  Photo  │  │  Photo  │  │  Photo  │ │ │ ← Image Gallery
│  │  │   [1]   │  │   [2]   │  │   [3]   │  │   [4]   │ │ │
│  │  └─────────┘  └─────────┘  └─────────┘  └─────────┘ │ │
│  │  ❤️ Favorite  📍 Tokyo     📷 Leica M6  🎞️ Portra   │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                               │
└─────────────────────────────────────────────────────────────┘

Year View (点击年份标题):
┌─────────────────────────────────────────────────────────────┐
│  2026                                                        │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────┬──────────┬──────────┬──────────┐             │
│  │   JAN    │   FEB    │   MAR    │   APR    │             │
│  │  ┌────┐  │  ┌────┐  │  ┌────┐  │  ┌────┐  │             │
│  │  │📸📸│  │  │📸  │  │  │📸📸│  │  │    │  │             │ ← Heat Map
│  │  │📸📸│  │  │📸📸│  │  │📸  │  │  │    │  │             │
│  │  └────┘  │  └────┘  │  └────┘  │  └────┘  │             │
│  │ 128 ph   │  96 ph   │  64 ph   │  0 ph    │             │
│  └──────────┴──────────┴──────────┴──────────┘             │
│                                                               │
│  [... rest of months ...]                                    │
└─────────────────────────────────────────────────────────────┘
```

#### HeroUI 组件映射

1. **Calendar** - 日历主体 (HeroUI 内置 Calendar 组件！)
   ```jsx
   import { Calendar } from "@heroui/calendar";
   import { parseDate, today, getLocalTimeZone } from "@internationalized/date";
   
   // HeroUI 提供完整的 Calendar 组件
   <Calendar
     value={selectedDate}
     onChange={setSelectedDate}
     showMonthAndYearPickers
     color="primary"
     classNames={{
       base: "bg-content1 rounded-large shadow-medium",
       cellButton: "data-[selected=true]:bg-primary",
     }}
     // 自定义单元格内容以显示照片缩略图
     renderCell={(date) => (
       <CalendarCell date={date} photos={photosByDay.get(date.toString())} />
     )}
   />
   ```

2. **自定义 CalendarCell** - 显示照片缩略图
   ```jsx
   function CalendarCell({ date, photos }) {
     const hasPhotos = photos && photos.length > 0;
     return (
       <div className="relative w-full h-full flex flex-col items-center">
         <span className="text-sm">{date.day}</span>
         {hasPhotos && (
           <Badge content={photos.length} color="warning" size="sm">
             <Image
               src={photos[0].thumb}
               alt=""
               className="w-8 h-8 object-cover rounded-sm"
             />
           </Badge>
         )}
       </div>
     );
   }
   ```

3. **Badge** - 照片数量标记
   ```jsx
   <Badge content={photoCount} color="warning" size="sm">
     <div className="calendar-cell" />
   </Badge>
   ```

4. **Tabs** - Timeline/Calendar 切换
   ```jsx
   <Tabs
     selectedKey={viewMode}
     onSelectionChange={setViewMode}
     color="primary"
     variant="underlined"
   >
     <Tab key="timeline" title="Timeline" />
     <Tab key="calendar" title="Life Log" />
   </Tabs>
   ```

5. **Modal** - 照片预览
   ```jsx
   <Modal size="5xl" isOpen={isOpen}>
     <ModalContent>
       <ModalHeader>January 3, 2026</ModalHeader>
       <ModalBody>
         <ImageGallery photos={selectedDayPhotos} />
       </ModalBody>
     </ModalContent>
   </Modal>
   ```

#### 交互设计

1. **日期点击**: 显示当天所有照片（Modal 或底部抽屉）
2. **照片 Hover**: 显示 EXIF 信息 Tooltip
3. **多选模式**: 长按启用多选，批量操作
4. **手势支持**: 左右滑动切换月份
5. **快速跳转**: 顶部下拉选择器快速跳转到任意年月

#### 动画设计

```jsx
// 日历网格动画
const gridVariants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: {
      staggerChildren: 0.02,
      delayChildren: 0.1
    }
  }
};

const cellVariants = {
  hidden: { opacity: 0, scale: 0.8 },
  visible: { 
    opacity: 1, 
    scale: 1,
    transition: { duration: 0.2 }
  }
};

// 照片加载动画
const photoVariants = {
  loading: { opacity: 0.3, scale: 0.95 },
  loaded: { 
    opacity: 1, 
    scale: 1,
    transition: { duration: 0.3 }
  }
};
```

#### 特殊功能

1. **回忆 (Memories)**
   ```jsx
   <Card className="mb-4" isBlurred>
     <CardHeader>
       <h3>📅 This Day in History</h3>
     </CardHeader>
     <CardBody>
       <p>3 years ago, you captured 12 photos at Tokyo Tower</p>
       <ImageGrid photos={memoryPhotos} />
     </CardBody>
   </Card>
   ```

2. **热力图 (Heatmap)**
   - 用 HeroUI 的 Progress + Chip 实现
   - 颜色从浅金到深金表示拍摄量

3. **统计卡片**
   ```jsx
   <div className="grid grid-cols-3 gap-4 mb-6">
     <Card>
       <CardBody>
         <p className="text-small text-default-500">Total Photos</p>
         <h2 className="text-3xl font-bold">1,284</h2>
       </CardBody>
     </Card>
     {/* More stats... */}
   </div>
   ```

---

### 🎨 Phase 2: Overview 改造

**当前状态**: 基础的 Hero 区域 + 过滤 + Rolls/Photos 网格

**改造目标**: Dashboard 式的首页体验

#### 设计稿

```
┌─────────────────────────────────────────────────────────────┐
│  Welcome back! 👋                                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  [Hero: Random Photos Carousel - 全屏宽度]          │   │ ← Swiper
│  │  ┌─────────┐┌─────────┐┌─────────┐┌─────────┐      │   │
│  │  │ Photo 1 ││ Photo 2 ││ Photo 3 ││ Photo 4 │      │   │
│  │  └─────────┘└─────────┘└─────────┘└─────────┘      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                               │
│  Quick Stats ────────────────────────────────────────────   │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐          │
│  │ 📷 128  │ │ 🎞️ 45  │ │ ❤️ 234  │ │ 🌍 12   │          │ ← Stat Cards
│  │ Rolls   │ │ Films   │ │ Favs    │ │ Places  │          │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘          │
│                                                               │
│  Recent Activity ─────────────────────────────────────────  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  🎞️ Kodak Portra 400  →  Canon AE-1  →  Tokyo      │   │ ← List Items
│  │      35mm · 36 exp · Jan 28, 2026                    │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │  🎞️ Ilford HP5 Plus   →  Leica M6   →  Kyoto       │   │
│  │      35mm · 36 exp · Jan 25, 2026                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                               │
│  Browse ──────────────────────────────────────────────────  │
│  [Rolls] [Photos] Toggle  [🔍 Search] [🎛️ Filters]         │
│  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐    │
│  │  [Roll Card]  │ │  [Roll Card]  │ │  [Roll Card]  │    │ ← Grid
│  └───────────────┘ └───────────────┘ └───────────────┘    │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

#### HeroUI 组件映射

1. **Hero Section**
   ```jsx
   <Card className="h-[400px] mb-6" isBlurred>
     <CardBody className="p-0">
       <Swiper>
         {randomPhotos.map(photo => (
           <SwiperSlide key={photo.id}>
             <Image src={photo.url} className="w-full h-full object-cover" />
           </SwiperSlide>
         ))}
       </Swiper>
     </CardBody>
   </Card>
   ```

2. **Stat Cards**
   ```jsx
   <Card shadow="sm" isPressable>
     <CardBody className="text-center">
       <div className="text-4xl mb-2">📷</div>
       <p className="text-3xl font-bold">{rollsCount}</p>
       <p className="text-small text-default-500">Rolls</p>
     </CardBody>
   </Card>
   ```

3. **Activity List**
   ```jsx
   <Card>
     <CardBody>
       <Listbox aria-label="Recent Activity">
         <ListboxItem
           key={roll.id}
           startContent={<Avatar src={roll.film_icon} />}
           description={roll.metadata}
         >
           {roll.name}
         </ListboxItem>
       </Listbox>
     </CardBody>
   </Card>
   ```

4. **Search & Filter**
   ```jsx
   <div className="flex gap-4 mb-6">
     <Input
       placeholder="Search rolls, films..."
       startContent={<SearchIcon />}
       isClearable
     />
     <Button 
       endContent={<FilterIcon />}
       variant="flat"
       onPress={openFilterDrawer}
     >
       Filters
     </Button>
   </div>
   ```

5. **Filter Drawer**
   ```jsx
   <Drawer isOpen={isFilterOpen} onClose={closeFilter}>
     <DrawerContent>
       <DrawerHeader>Filters</DrawerHeader>
       <DrawerBody>
         <Select label="Film Type">
           <SelectItem key="all">All Films</SelectItem>
           <SelectItem key="color">Color</SelectItem>
           <SelectItem key="bw">Black & White</SelectItem>
         </Select>
         <CheckboxGroup label="Format">
           <Checkbox value="35mm">35mm</Checkbox>
           <Checkbox value="120">120</Checkbox>
         </CheckboxGroup>
       </DrawerBody>
     </DrawerContent>
   </Drawer>
   ```

---

### ⚠️ 暂缓改造: FilmLab

**原因分析**:

1. **极高的复杂度**
   - 2500+ 行代码的核心组件
   - 深度集成 WebGL 渲染管线
   - 复杂的状态管理（40+ useState）
   - 自定义滑块、曲线编辑器等专业控件

2. **风险过高**
   - UI 改造可能影响渲染精度
   - 性能敏感（实时预览）
   - 大量边界情况需要测试

3. **投入产出比低**
   - 现有 UI 虽然传统但功能完整
   - 用户学习成本已固定
   - 改造工期可能超过 4 周

**建议策略**:
- Phase 1-3 完成后，评估是否进行 FilmLab 改造
- 可先进行小范围美化（按钮、颜色、间距）
- 保持核心交互逻辑不变

---

### 📦 Phase 3: 其他组件改造

#### RollDetail 改造

**现有问题**: 侧边栏样式老旧，照片网格单调

**改造方案**:

```jsx
// Roll Header
<Card className="mb-6">
  <CardBody>
    <div className="flex justify-between items-start">
      <div>
        <Chip size="sm" variant="flat">{roll.format}</Chip>
        <h1 className="text-3xl font-bold mt-2">{roll.name}</h1>
        <p className="text-default-500">{roll.film_name}</p>
      </div>
      <Avatar src={roll.camera_icon} size="lg" />
    </div>
    <Divider className="my-4" />
    <div className="flex gap-4">
      <Chip startContent={<CalendarIcon />}>{roll.shot_date}</Chip>
      <Chip startContent={<CameraIcon />}>{roll.camera_name}</Chip>
      <Chip startContent={<LocationIcon />}>{roll.location}</Chip>
    </div>
  </CardBody>
</Card>

// Photo Grid
<div className="grid grid-cols-4 gap-4">
  {photos.map(photo => (
    <Card key={photo.id} isPressable shadow="sm">
      <CardBody className="p-0">
        <Image src={photo.thumb} className="w-full aspect-square" />
      </CardBody>
      <CardFooter>
        <Button isIconOnly size="sm" variant="light">
          <HeartIcon />
        </Button>
      </CardFooter>
    </Card>
  ))}
</div>
```

#### FilmLibrary 改造

**改造重点**: 表格 → 现代卡片布局

```jsx
// Inventory Filter
<Tabs selectedKey={statusFilter} onSelectionChange={setStatusFilter}>
  <Tab key="all" title={<Chip>All ({allCount})</Chip>} />
  <Tab key="in-stock" title={<Chip color="success">In Stock ({inStockCount})</Chip>} />
  <Tab key="loaded" title={<Chip color="warning">Loaded ({loadedCount})</Chip>} />
  <Tab key="used" title={<Chip color="default">Used ({usedCount})</Chip>} />
</Tabs>

// Film Item Card
<Card className="mb-4">
  <CardBody>
    <div className="flex gap-4">
      <Avatar src={film.icon} size="lg" />
      <div className="flex-1">
        <h3 className="font-bold">{film.name}</h3>
        <p className="text-small text-default-500">{film.format} · ISO {film.iso}</p>
        <Chip size="sm" color={statusColor}>{film.status}</Chip>
      </div>
      <Button size="sm" variant="flat">Load Roll</Button>
    </div>
  </CardBody>
</Card>
```

#### Settings 改造

```jsx
<Tabs aria-label="Settings">
  <Tab key="general" title="General">
    <Card>
      <CardBody>
        <Select label="Theme">
          <SelectItem key="light">Light</SelectItem>
          <SelectItem key="dark">Dark</SelectItem>
          <SelectItem key="auto">Auto</SelectItem>
        </Select>
        <Input label="Storage Path" isReadOnly value={storagePath} />
        <Switch>Write-through Mode</Switch>
      </CardBody>
    </Card>
  </Tab>
  <Tab key="luts" title="LUTs">
    <LutLibrary />
  </Tab>
</Tabs>
```

---

## 组件库迁移

### 全局组件替换清单

| 旧组件 | HeroUI 替代 | 优先级 |
|-------|------------|-------|
| `<button>` | `<Button>` | ⭐⭐⭐⭐⭐ |
| `<input>` | `<Input>` | ⭐⭐⭐⭐⭐ |
| `<select>` | `<Select>` | ⭐⭐⭐⭐⭐ |
| Custom Modal | `<Modal>` | ⭐⭐⭐⭐⭐ |
| Custom Card | `<Card>` | ⭐⭐⭐⭐⭐ |
| Custom Tabs | `<Tabs>` | ⭐⭐⭐⭐ |
| `<img>` | `<Image>` | ⭐⭐⭐⭐ |
| Custom Dropdown | `<Dropdown>` | ⭐⭐⭐ |
| Custom Tooltip | `<Tooltip>` | ⭐⭐⭐ |
| Loading Spinner | `<Spinner>` | ⭐⭐⭐ |

### 创建基础组件库

```
client/src/components/ui/
├── Button.jsx (扩展 HeroUI Button)
├── Input.jsx
├── Card.jsx
├── Modal.jsx
├── Layout/
│   ├── Container.jsx
│   ├── Section.jsx
│   └── Grid.jsx
├── Navigation/
│   ├── Sidebar.jsx (改造后)
│   ├── Tabs.jsx
│   └── Breadcrumbs.jsx
└── Feedback/
    ├── Toast.jsx
    ├── Alert.jsx
    └── Progress.jsx
```

### 样式系统迁移

#### 1. CSS Variables → Tailwind

```css
/* 旧 CSS */
.card {
  background: var(--color-bg-alt);
  border: 1px solid var(--color-border);
  border-radius: 12px;
  padding: 20px;
}

/* 新 Tailwind */
<Card className="bg-content1 border-divider rounded-large p-5" />
```

#### 2. 动画统一

```jsx
// 创建全局动画配置
// client/src/config/animations.js
export const fadeIn = {
  initial: { opacity: 0, y: 20 },
  animate: { opacity: 1, y: 0 },
  exit: { opacity: 0, y: -20 },
  transition: { duration: 0.3 }
};

export const scaleIn = {
  initial: { opacity: 0, scale: 0.8 },
  animate: { opacity: 1, scale: 1 },
  transition: { type: "spring", stiffness: 200 }
};
```

---

## 实施路线图

### 📍 里程碑概览

| 阶段 | 时间 | 核心目标 | 关键产出 | 状态 |
|-----|------|---------|---------|------|
| **Phase 0** | Week 1 | 基础设施搭建 | Tailwind v4 + HeroUI 配置完成 | ✅ 完成 |
| **Phase 1** | Week 2-3 | 核心页面改造 | Timeline + Life Log + Sidebar | ✅ 完成 |
| **Phase 2** | Week 4-5 | 主要功能改造 | Overview + RollDetail | ✅ 完成 |
| **Phase 3** | Week 6-7 | 剩余页面改造 | FilmLibrary + Settings + Statistics | 🔄 部分完成 (仅 Statistics 组件化) |
| **Phase 4** | Week 8+ | 优化与收尾 | 深色模式 + 动画 + 全页面改造 | 🔲 待开始 |

---

### Week 1: Phase 0 - 基础设施 (Foundation)

**目标**: 完成 HeroUI + Tailwind 安装配置，不破坏现有功能

**状态**: ✅ 已完成 (2026-01-29)

**技术说明**: 使用 **Tailwind CSS v4** (CSS-first 配置方式)，不再需要 `tailwind.config.js`

**任务清单**:
- [x] 安装 HeroUI 核心依赖 (`@heroui/react`, `@heroui/theme`, `lucide-react`, `@internationalized/date`)
- [x] 配置 Tailwind CSS v4 + `@tailwindcss/postcss` + CRACO
- [x] 创建 `postcss.config.js` 配置 (使用 @tailwindcss/postcss)
- [x] 创建 `styles/tailwind.css` 完整配置 (Tailwind v4 CSS-first 方式，含 @theme 指令)
- [x] 创建 HeroUIProvider 包装器 (`providers/HeroUIProvider.jsx`)
- [x] 创建基础 UI 组件目录结构 (`components/ui/`)
- [x] 更新 App.js 集成 HeroUIProvider
- [x] 更新 index.js 导入 tailwind.css
- [x] 验证编译通过 (webpack compiled successfully)

**产出文件**:
- `client/postcss.config.js` - PostCSS 配置 (使用 @tailwindcss/postcss)
- `client/craco.config.js` - 更新的 CRACO 配置 (PostCSS file mode + @ui, @providers 别名)
- `client/src/styles/tailwind.css` - Tailwind v4 CSS-first 配置 (含 @theme, @utility 指令)
- `client/src/providers/HeroUIProvider.jsx` - HeroUI 提供者 + 主题系统
- `client/src/providers/index.js` - 统一导出
- `client/src/components/ui/index.js` - UI 组件统一导出
- `client/src/components/ui/Button.jsx` - 增强按钮组件
- `client/src/components/ui/Card.jsx` - 增强卡片组件 (含 PhotoCard, StatCard)
- `client/src/components/ui/Skeleton.jsx` - 骨架屏系列组件
- `client/src/components/ui/AnimatedContainer.jsx` - Framer Motion 动画封装
- `client/src/components/ui/icons.js` - Lucide React 图标统一导出

**验收标准**:
```jsx
// App.js 能正常运行以下代码
import { Button } from "@heroui/button";

<Button color="primary">Test HeroUI</Button>
```

---

### Week 2-3: Phase 1 - 核心页面 (Critical Path)

**目标**: 改造用户最常用的 Timeline、Life Log 和全局 Sidebar

**状态**: ✅ 已完成 (2026-01-29)

#### Week 2: Sidebar + CalendarView 切换器

- [x] **Sidebar 现代化** ✅
  - [x] 创建模块化 Sidebar 组件 (`components/Sidebar/`)
  - [x] SidebarContext 管理折叠状态 (localStorage 持久化)
  - [x] SidebarItem 使用 HeroUI Button + Tooltip
  - [x] SidebarSection 使用 HeroUI Divider
  - [x] 添加 Avatar 组件显示应用图标
  - [x] 添加主题切换 (日/夜模式)
  - [x] 实现折叠/展开动画 (Framer Motion)
  - [x] 使用 Lucide React 图标库
  
- [x] **CalendarView 切换器** ✅
  - [x] 使用 HeroUI Tabs 替换自定义按钮
  - [x] 添加 Timeline / Life Log 切换图标
  - [x] 添加切换动画

#### Week 3: Timeline + PhotoCalendar

- [x] **TimelineView 重构** ✅
  - [x] 创建 `components/Timeline/` 目录结构
  - [x] TimelineContext 管理状态 (年月选择、rolls 数据、颜色调色板)
  - [x] TimelineFilters 使用 HeroUI Button + Chip 实现年月过滤
  - [x] TimelineMonthGrid 使用 Card 实现月份时间线视图
  - [x] TimelineCalendarGrid 实现日历网格视图
  - [x] TimelineRollGrid 使用 Card 实现照片卡片网格
  - [x] 添加 stagger 动画效果 (Framer Motion)

- [x] **PhotoCalendar (Life Log) 重构** ✅
  - [x] 创建 `components/LifeLog/` 目录结构
  - [x] LifeLogContext 管理状态 (日期导航、照片数据、封面偏好)
  - [x] LifeLogMonthGrid 实现月份日历网格
  - [x] LifeLogYearGrid 使用 Card 实现年度总览
  - [x] LifeLogDayModal 使用 HeroUI Modal 显示当日照片
  - [x] 支持月视图/年视图切换
  - [x] 添加封面照片切换功能

**产出文件**:
- `client/src/components/Sidebar/index.js` - 模块导出
- `client/src/components/Sidebar/SidebarContext.jsx` - 状态管理
- `client/src/components/Sidebar/SidebarItem.jsx` - 导航项组件
- `client/src/components/Sidebar/SidebarSection.jsx` - 分组组件
- `client/src/components/Sidebar/Sidebar.jsx` - 主组件
- `client/src/components/Timeline/index.js` - 模块导出
- `client/src/components/Timeline/TimelineContext.jsx` - 状态管理
- `client/src/components/Timeline/TimelineFilters.jsx` - 年月过滤器
- `client/src/components/Timeline/TimelineMonthGrid.jsx` - 月份时间线
- `client/src/components/Timeline/TimelineCalendarGrid.jsx` - 日历网格
- `client/src/components/Timeline/TimelineRollGrid.jsx` - 照片卡片网格
- `client/src/components/Timeline/TimelineView.jsx` - 主视图
- `client/src/components/LifeLog/index.js` - 模块导出
- `client/src/components/LifeLog/LifeLogContext.jsx` - 状态管理
- `client/src/components/LifeLog/LifeLogMonthGrid.jsx` - 月份日历
- `client/src/components/LifeLog/LifeLogYearGrid.jsx` - 年度总览
- `client/src/components/LifeLog/LifeLogDayModal.jsx` - 日期详情弹窗
- `client/src/components/LifeLog/LifeLogView.jsx` - 主视图
- `client/src/components/CalendarView.jsx` - 更新使用新组件

**产出**:
- 全新 Timeline 视图 (模块化、HeroUI组件)
- 全新 Life Log 视图 (模块化、HeroUI组件)
- 现代化 Sidebar (HeroUI + Framer Motion)
- 流畅动画效果

---

### Week 4-5: Phase 2 - 主要功能 (Major Features)

**目标**: Overview 首页 + RollDetail 详情页

#### Week 4: Overview 改造

**状态**: ✅ 已完成 (2026-01-29)

- [x] **Hero Section**
  - [x] 使用 Card + Image 实现照片轮播 (HeroCarousel)
  - [x] 添加渐变遮罩和文字叠加
  - [x] 自动轮播 + 手动导航
  - [x] 刷新/全屏按钮
  
- [x] **Quick Stats**
  - [x] 使用 Card 实现统计卡片 (QuickStats)
  - [x] 显示 Rolls/Photos/Favorites/Locations/Films 数量
  - [x] 添加 hover 动画效果
  - [x] 骨架屏加载状态
  
- [x] **Browse Section**
  - [x] 使用 Tabs 替换 Rolls/Photos 切换 (BrowseSection)
  - [x] 使用 Input 实现搜索框
  - [x] 使用 Modal 实现过滤器侧边栏 (FilterDrawer)
  - [x] 活动过滤器 Chip 展示
  - [x] 清除过滤器功能

**产出文件**:
- `client/src/components/Overview/index.js` - 模块导出
- `client/src/components/Overview/OverviewView.jsx` - 主视图容器
- `client/src/components/Overview/HeroCarousel.jsx` - 照片轮播组件
- `client/src/components/Overview/QuickStats.jsx` - 统计卡片组件
- `client/src/components/Overview/BrowseSection.jsx` - 浏览区域组件
- `client/src/components/Overview/FilterDrawer.jsx` - 过滤器弹窗组件

#### Week 5: RollDetail 改造

**状态**: ✅ 已完成 (2026-01-29)

- [x] **Roll Header**
  - [x] 创建 `components/RollDetail/` 目录结构
  - [x] 使用 Card 包装头部信息 (RollHeader)
  - [x] 使用 Chip 显示标签信息 (roll number, format)
  - [x] 使用图标+文字显示 Date/Film/Camera/Lens/Photographer
  - [x] Locations 使用 Chip 展示
  - [x] Notes 区域样式优化
  
- [x] **Roll Toolbar**
  - [x] 创建 RollToolbar 组件
  - [x] View mode 切换 (Positive/Negative)
  - [x] Multi-select 按钮
  - [x] Upload 按钮
  - [x] Batch 下拉菜单 (Render/Download/Contact Sheet)
  - [x] Import 下拉菜单 (Positives/RAW)

- [x] **Photo Grid**
  - [x] 创建 RollPhotoGrid 组件
  - [x] 使用 Card + Image 实现照片卡片
  - [x] 添加 hover 效果和操作按钮 (favorite, cover, delete, tags)
  - [x] 选择模式支持 (Checkbox)
  - [x] Lazy loading 懒加载
  - [x] 骨架屏加载状态
  - [x] 空状态展示
  
- [x] **Edit Drawer (Sidebar)**
  - [x] 创建 RollEditDrawer 组件
  - [x] 使用 Modal 作为右侧滑入抽屉
  - [x] 使用 Accordion 分组表单 (Basic Info, Locations, Development, Scanning)
  - [x] 使用 HeroUI 组件替换表单元素 (Input, Select, Textarea)
  - [x] 集成 EquipmentSelector 和 LocationSelect

**产出文件**:
- `client/src/components/RollDetail/index.js` - 模块导出
- `client/src/components/RollDetail/RollHeader.jsx` - 头部信息卡片
- `client/src/components/RollDetail/RollToolbar.jsx` - 工具栏组件
- `client/src/components/RollDetail/RollPhotoGrid.jsx` - 照片网格组件
- `client/src/components/RollDetail/RollEditDrawer.jsx` - 编辑抽屉组件

**注意**: RollDetail 组件较大（895行），采用渐进式迁移策略，新组件可独立使用
- 优化的胶卷详情页

---

### Week 6-7: Phase 3 - 剩余页面 (Remaining Pages)

**目标**: 完成所有页面迁移

**当前状态**: 🔄 部分完成 (2026-01-30)

#### 已完成项 ✅

- [x] **Statistics 组件化**
  - [x] 创建 `components/Statistics/` 模块
  - [x] StatCard.jsx - 统计卡片组件
  - [x] ChartCard.jsx - 图表卡片包装
  - [x] StatsModeToggle.jsx - 模式切换组件
  - [x] 保留 recharts 图表
  - [x] 使用 Lucide React 图标

#### 待完成项 🔲

**Week 6: FilmLibrary + EquipmentManager**

- [ ] **FilmLibrary 改造**
  - [ ] 创建 `components/FilmLibrary/` 模块
  - [ ] 使用 Tabs 实现状态过滤 (all/in-stock/loaded/used)
  - [ ] 使用 Card 实现库存卡片 (FilmItemCard)
  - [ ] 使用 Modal 实现批量添加/编辑弹窗
  - [ ] 删除 `FilmInventory.css`, `FilmButtons.css`
  
- [ ] **EquipmentManager 模块化拆分**
  - [ ] 拆分 1076 行巨型组件为 10+ 子组件
  - [ ] 创建 `components/EquipmentManager/` 模块
  - [ ] 使用 Tabs 分类展示 (Cameras/Lenses/Flashes/Film Backs/Scanners/Films)
  - [ ] 使用 Card 实现设备卡片 (EquipmentCard 通用组件)
  - [ ] 使用 Drawer 展示关联胶卷 (RelatedRollsDrawer)
  - [ ] 删除 `EquipmentManager.css`

**Week 7: Settings + Favorites + TagGallery**

- [ ] **Settings 改造**
  - [ ] 升级 SettingsTabs 为 HeroUI Tabs
  - [ ] 新增 AppearanceSettings 页面 (主题、颜色、布局)
  - [ ] 使用 Input/Select/Switch 替换表单 (HeroUI 组件)
  
- [ ] **Favorites 改造**
  - [ ] 创建 FavoriteGrid 组件
  - [ ] 使用 Card + Image 样式
  - [ ] 添加动画效果 (AnimatedContainer)
  
- [ ] **TagGallery 改造**
  - [ ] 创建 TagCloud 组件 (使用 Chip)
  - [ ] 照片网格使用 Card
  - [ ] 添加动画效果

**产出**:
- 全站统一 UI (延期到 Phase 4 完成)

---

### Week 8+: Phase 4 - 优化与收尾 (Polish)

**目标**: 深色模式、动画优化、全页面改造、性能测试

**扩展任务** (基于 UI-IMPROVEMENT-RECOMMENDATIONS.md):

- [ ] **深色模式全局优化**
  - [ ] 在 Settings 中添加主题切换器 (已有 ThemeToggle，需集成到 AppearanceSettings)
  - [ ] Recharts 图表深色模式适配 (ChartWrapper)
  - [ ] 图片在深色模式下的优化 (DarkModeImage)
  - [ ] 地图组件深色模式 (MapPage)
  - [ ] 测试所有页面的深色模式表现
  
- [ ] **动画系统统一**
  - [ ] 扩展动画库 (animations.js - fadeIn, scaleIn, slideIn, staggerContainer)
  - [ ] 创建高阶动画组件 (AnimatedList, AnimatedCard, PageTransition)
  - [ ] 添加微交互 (RippleButton, LoadingSpinner, Toast)
  - [ ] 应用 PageTransition 到路由切换
  
- [ ] **组件库扩展**
  - [ ] Breadcrumbs (面包屑导航)
  - [ ] EmptyState (空状态占位)
  - [ ] Pagination (分页器)
  - [ ] ContextMenu (右键菜单)
  - [ ] OptimizedImage (图片优化组件)
  - [ ] VirtualGrid (虚拟滚动网格)
  - [ ] 更新 `components/ui/index.js` 统一导出
  
- [ ] **性能优化**
  - [ ] 路由懒加载优化 (React.lazy + Suspense)
  - [ ] 图片懒加载与渐进式加载
  - [ ] 虚拟滚动应用到所有大列表
  - [ ] 代码分割与打包优化
  
- [ ] **CSS 清理**
  - [ ] 删除 `sidebar.css`, `roll-detail-card.css` (已被组件替代)
  - [ ] 迁移 `forms.css` 到 Tailwind utilities
  - [ ] 整合 `equipment-selector.css`, `FilmInventory.css`, `FilmButtons.css`
  - [ ] 保留必要的 `map.css`, `styles.css` (简化版)
  
- [ ] **渐进增强功能 (可选)**
  - [ ] 快捷键系统 (useKeyboardShortcuts hook)
  - [ ] 拖拽排序 (SortableList with @dnd-kit)
  - [ ] 搜索高亮 (HighlightText component)
  
- [ ] **测试与修复**
  - [ ] 全功能回归测试
  - [ ] 响应式测试 (不同分辨率)
  - [ ] 深色模式全页面测试
  - [ ] 性能基准测试 (首次加载、页面切换、滚动 FPS)
  - [ ] Bug 修复

- [ ] **文档更新**
  - [ ] 更新 UI-IMPROVEMENT-RECOMMENDATIONS.md
  - [ ] 更新开发者文档
  - [ ] 记录组件使用方式

**产出**:
- 完整的深色模式
- 流畅的动画系统
- 扩展的组件库
- 优化的性能
- 稳定的生产版本

**参考文档**: 详细改进方案见 [UI-IMPROVEMENT-RECOMMENDATIONS.md](./UI-IMPROVEMENT-RECOMMENDATIONS.md)

---

## 风险评估与缓解策略

### 🔴 高风险

| 风险 | 影响 | 概率 | 缓解策略 |
|-----|------|-----|---------|
| **样式冲突** | 现有CSS与Tailwind冲突导致UI错乱 | 高 | 使用 `prefix: "heroui"` 配置；渐进式迁移，保留旧CSS |
| **Electron 兼容性** | HeroUI 动画在 Electron 中表现异常 | 中 | 提前在 Electron 环境测试；准备 `disableAnimation` 回退 |
| **包体积膨胀** | 添加大量依赖导致应用启动变慢 | 中 | 使用按需导入；启用 Tree-shaking；监控 bundle 大小 |

### 🟡 中风险

| 风险 | 影响 | 概率 | 缓解策略 |
|-----|------|-----|---------|
| **React 18 并发问题** | HeroUI 组件与现有代码的并发渲染冲突 | 低 | 保持 React 18 版本；测试关键交互流程 |
| **framer-motion 版本** | 项目已有 framer-motion，版本冲突 | 中 | 检查版本兼容性，统一到最新版本 |
| **虚拟滚动整合** | react-window 与 HeroUI Card 整合困难 | 中 | 保持 react-window，仅替换内部渲染组件 |

### 🟢 低风险

| 风险 | 影响 | 概率 | 缓解策略 |
|-----|------|-----|---------|
| **学习曲线** | 开发者不熟悉 HeroUI API | 低 | 文档完善，API 直观 |
| **深色模式适配** | 图片在深色模式下显示问题 | 低 | 使用 `mix-blend-mode` 或调整背景 |

### 回退策略

如果迁移过程中遇到无法解决的问题：

1. **短期**: 使用 feature flag 控制新旧 UI 切换
   ```jsx
   const useNewUI = localStorage.getItem('useNewUI') === 'true';
   return useNewUI ? <NewTimeline /> : <LegacyTimeline />;
   ```

2. **中期**: 保持双轨运行，逐步验证稳定性

3. **长期回退**: 如果 HeroUI 不适合，考虑：
   - Radix UI + Tailwind (更底层的控制)
   - shadcn/ui (组件代码可完全自定义)

---

## 渐进式迁移策略

为了降低风险，采用 **渐进式迁移** 而非大爆炸式重写：

### 阶段 1: 共存期

```
client/src/components/
├── ui/                    # 新 HeroUI 组件
│   ├── Button.jsx
│   ├── Card.jsx
│   └── ...
├── TimelineView.jsx       # 旧组件（待替换）
├── TimelineViewNew.jsx    # 新组件（开发中）
└── ...
```

### 阶段 2: 替换期

```jsx
// 使用条件渲染进行 A/B 测试
import { TimelineView } from './TimelineView';
import { TimelineViewNew } from './TimelineViewNew';

function CalendarPage() {
  const experimentGroup = useExperiment('new-timeline');
  
  return experimentGroup === 'new' 
    ? <TimelineViewNew /> 
    : <TimelineView />;
}
```

### 阶段 3: 清理期

- 删除旧组件
- 删除旧 CSS
- 合并重复代码
- 更新文档

---

## 测试策略

### 单元测试 (可选)

如果时间允许，为关键组件添加测试：

```jsx
// __tests__/Timeline.test.jsx
import { render, screen } from '@testing-library/react';
import { TimelineView } from '../TimelineView';

test('renders year sections', () => {
  render(<TimelineView />);
  expect(screen.getByText('2026')).toBeInTheDocument();
});
```

### 手动测试清单

每个 Phase 完成后执行：

- [ ] 所有页面可正常访问
- [ ] 所有按钮点击响应正常
- [ ] 表单提交功能正常
- [ ] 照片加载和显示正常
- [ ] 模态框打开/关闭正常
- [ ] Electron 窗口操作正常 (最小化/最大化/关闭)
- [ ] 拖拽窗口正常
- [ ] 快捷键正常

### 性能基准

记录改造前后的关键指标：

| 指标 | 改造前 | 目标 | 改造后 |
|-----|-------|-----|-------|
| 首次加载时间 | ___ ms | < 3000 ms | ___ ms |
| Timeline 渲染 | ___ ms | < 500 ms | ___ ms |
| 照片网格滚动 | ___ fps | > 30 fps | ___ fps |
| 打包体积 | ___ KB | < +200 KB | ___ KB |

---

## HeroUIProvider 配置

```jsx
// client/src/providers/HeroUIProvider.jsx
import { HeroUIProvider as Provider } from "@heroui/react";
import { useNavigate } from "react-router-dom";

export function HeroUIProvider({ children }) {
  const navigate = useNavigate();

  return (
    <Provider 
      navigate={navigate}
      // 减少动画在 Electron 中的问题
      disableAnimation={window.__electron?.isLowPerf}
    >
      {children}
    </Provider>
  );
}
```

```jsx
// client/src/App.js 更新
import { HeroUIProvider } from './providers/HeroUIProvider';

function App() {
  return (
    <QueryClientProvider client={queryClient}>
      <Router>
        <HeroUIProvider>
          <Layout />
        </HeroUIProvider>
      </Router>
    </QueryClientProvider>
  );
}
```

### 代码拆分

```jsx
// Lazy load heavy components
const FilmLab = lazy(() => import('./components/FilmLab/FilmLab'));
const MapPage = lazy(() => import('./pages/MapPage'));

// Use Suspense
<Suspense fallback={<Spinner size="lg" />}>
  <FilmLab />
</Suspense>
```

### 图片优化

```jsx
// 使用 HeroUI Image 的懒加载
<Image
  src={photoUrl}
  loading="lazy"
  placeholder="blur" // 如果可能
  className="object-cover"
/>
```

### 虚拟滚动

```jsx
// 对于大量照片，继续使用 react-window
import { FixedSizeGrid } from 'react-window';

<FixedSizeGrid
  columnCount={4}
  rowCount={Math.ceil(photos.length / 4)}
  // ...
>
  {({ columnIndex, rowIndex, style }) => (
    <div style={style}>
      <Card>
        <Image src={photos[rowIndex * 4 + columnIndex].url} />
      </Card>
    </div>
  )}
</FixedSizeGrid>
```

---

## 附录

### HeroUI 核心组件速查

| 组件 | 用途 | 文档 |
|-----|------|------|
| Button | 按钮 | [Link](https://heroui.com/docs/components/button) |
| Input | 输入框 | [Link](https://heroui.com/docs/components/input) |
| Select | 下拉选择 | [Link](https://heroui.com/docs/components/select) |
| Modal | 模态框 | [Link](https://heroui.com/docs/components/modal) |
| Card | 卡片 | [Link](https://heroui.com/docs/components/card) |
| Tabs | 标签页 | [Link](https://heroui.com/docs/components/tabs) |
| Accordion | 折叠面板 | [Link](https://heroui.com/docs/components/accordion) |
| Image | 图片 | [Link](https://heroui.com/docs/components/image) |
| Chip | 标签 | [Link](https://heroui.com/docs/components/chip) |
| Avatar | 头像 | [Link](https://heroui.com/docs/components/avatar) |
| Slider | 滑块 | [Link](https://heroui.com/docs/components/slider) |
| Switch | 开关 | [Link](https://heroui.com/docs/components/switch) |
| Drawer | 抽屉 | [Link](https://heroui.com/docs/components/drawer) |
| Progress | 进度条 | [Link](https://heroui.com/docs/components/progress) |

### Tailwind 实用类速查

```css
/* 间距 */
p-4, m-4, gap-4, space-y-4

/* 布局 */
flex, grid, grid-cols-4, justify-between, items-center

/* 尺寸 */
w-full, h-screen, max-w-7xl

/* 圆角 */
rounded-lg, rounded-full

/* 阴影 */
shadow-md, shadow-lg

/* 文字 */
text-lg, font-bold, text-center, text-default-500

/* 颜色 */
bg-primary, text-primary, border-divider

/* 响应式 */
sm:grid-cols-2, md:grid-cols-3, lg:grid-cols-4
```

### 设计资源

- **图标**: Lucide React (https://lucide.dev/icons/)
- **字体**: Inter / SF Pro (Apple 风格)
- **配色**: Coolors.co, Realtime Colors
- **动画**: Framer Motion (https://www.framer.com/motion/)
- **灵感**: Dribbble, Behance (搜索 "photo app UI")

---

## 总结

此方案将 FilmGallery 桌面端从传统 CSS 界面升级为现代化、组件化的设计系统。

### 核心优势

1. ✅ **统一的设计语言** - HeroUI 提供一致的视觉体验
2. ✅ **流畅的动画** - Framer Motion 原生集成
3. ✅ **深色模式支持** - 内置主题系统
4. ✅ **更好的可维护性** - 组件化开发
5. ✅ **更快的开发速度** - 无需手写 CSS
6. ✅ **更好的性能** - Tailwind JIT + Tree-shaking
7. ✅ **现代美学** - 符合 2026 年设计趋势

### 改造范围

| 改造 | 暂缓 |
|-----|-----|
| Timeline | FilmLab (复杂度极高) |
| PhotoCalendar (Life Log) | |
| Sidebar | |
| Overview | |
| RollDetail | |
| FilmLibrary | |
| Statistics | |
| Settings | |
| Favorites / Themes | |

### 时间估算

- **总开发时间**: **8 周** (全职工作)
- **Phase 0 (基础)**: 1 周
- **Phase 1 (核心)**: 2 周
- **Phase 2 (主要)**: 2 周
- **Phase 3 (剩余)**: 2 周
- **Phase 4 (优化)**: 1 周

### 成功标准

1. 所有改造页面视觉一致性达到 95%+
2. 用户交互响应时间 < 100ms
3. 无功能回退
4. 打包体积增长 < 200KB
5. 深色模式全功能支持

### 下一步行动

1. **确认方案**: 审阅此文档，确认改造范围和时间线
2. **环境准备**: 执行 Phase 0 任务，搭建基础设施
3. **原型验证**: 先在 Timeline 组件上验证 HeroUI 效果
4. **全面推进**: 按计划执行 Phase 1-4

---

**Created**: 2026-01-29
**Updated**: 2026-01-29
**Author**: AI Design System Architect
**Version**: 2.0.0
**Branch**: `feature/desktop-ui-modernization`
