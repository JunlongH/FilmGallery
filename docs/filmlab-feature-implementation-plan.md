# FilmLab 功能实现计划

**创建日期**: 2026-01-15  
**状态**: � 实施中  
**优先级**: RAW 解码 > 批量导出 > HSL > 分离色调扩展 > 自动边缘检测

---

## 功能审计结果

### ✅ 已完成功能 (15/17)

| 功能 | 状态 | 位置 |
|------|------|------|
| 胶片基底采样 | ✅ | FilmLab.jsx - 采样器工具 |
| RGB 通道增益 | ✅ | FilmLab.jsx - 白平衡面板 |
| Log/Tone 映射 | ✅ | filmlab-shared - buildToneLUT |
| 自动白平衡 | ✅ | FilmLab.jsx - 滴管工具 |
| 色温/色调 | ✅ | filmLabWhiteBalance.js - Kelvin 模型 |
| 曲线调整 | ✅ | filmLabCurves.js - Fritsch-Carlson 样条 |
| 3D LUT | ✅ | filmlab-core.js - 三线性插值 |
| 胶片预设 | ✅ | filmLabConstants.js - 16 种预设 |
| 裁剪/旋转 | ✅ | FilmLab.jsx - CropOverlay 组件 |
| 批量预设应用 | ✅ | 已有预设保存/加载功能 |
| Before/After | ✅ | FilmLab.jsx - 对比按钮 |
| H&D 密度曲线 | ✅ | filmLabCurve.js - applyFilmCurve |
| **HSL 调整** | ✅ | **filmLabHSL.js - 8 色相分区 (2026-01-15)** |
| **分离色调** | ✅ | **filmLabSplitTone.js - 高光/阴影着色 (2026-01-15)** |
| **后台导出队列** | ✅ | **export-queue.js + export.js (2026-01-15)** |

### ⚠️ 部分实现 (1/17)

| 功能 | 状态 | 说明 |
|------|------|------|
| 自动边缘检测 | ⚠️ | 仅手动裁剪，无自动底片边框检测 |

### ❌ 未实现 (1/17)

| 功能 | 优先级 | 说明 |
|------|--------|------|
| RAW 解码 | 🔴 高 | 需支持 DNG/CR2/ARW 等格式 |

---

## 模块实现计划

### 模块 1: RAW 解码器 (优先级: 🔴 高)

**目标**: 支持数码相机 RAW 格式 (DNG, CR2, ARW, NEF, ORF, RAF)

**文件结构**:
```
server/
  services/
    raw-decoder.js        # RAW 解码服务
  routes/
    raw.js                # RAW 相关 API 端点
packages/shared/
  rawFormats.js           # RAW 格式常量定义
```

**技术方案**:
- 使用 `dcraw` 或 `libraw` (通过 node-addon 或 child_process)
- 备选: `sharp` 结合 `libvips` 的 RAW 支持
- 输出: 线性 16-bit TIFF 供后续 Pipeline 处理

**API 设计**:
```javascript
// POST /api/raw/decode
{
  filePath: string,
  options: {
    colorSpace: 'sRGB' | 'AdobeRGB' | 'ProPhotoRGB',
    whiteBalance: 'camera' | 'daylight' | 'custom',
    demosaic: 'AHD' | 'PPG' | 'VNG',
    outputBits: 16
  }
}

// Response
{
  success: true,
  tempPath: '/tmp/raw_decoded_xxx.tiff',
  metadata: { width, height, camera, lens, iso, ... }
}
```

**预计工时**: 2-3 天

---

### 模块 2: HSL 调整 (优先级: 🔴 高) ✅ 已完成

**完成日期**: 2026-01-15

**实现文件**:
- `packages/shared/filmLabHSL.js` - CommonJS 版本
- `client/src/utils/filmlab-shared/filmLabHSL.js` - ESM 版本

**实现功能**:
- 8 色相分区 (红/橙/黄/绿/青/蓝/紫/品红)
- 每通道独立的色相偏移/饱和度/明度调整
- 余弦平滑过渡算法，避免色带
- RGB ↔ HSL 颜色空间转换
- 批量像素处理 `applyHSLToArray()`

**待完成**:
- [ ] 创建 `HSLPanel.jsx` UI 组件
- [ ] 集成到 WebGL 着色器

---

### 模块 3: 分离色调扩展 (优先级: 🟡 中) ✅ 已完成

**完成日期**: 2026-01-15

**实现文件**:
- `packages/shared/filmLabSplitTone.js` - CommonJS 版本
- `client/src/utils/filmlab-shared/filmLabSplitTone.js` - ESM 版本

**实现功能**:
- 高光/阴影分区着色
- 色相/饱和度参数
- 平衡滑块控制过渡
- 5 种预设 (暖冷胶片、青橙电影、复古褪色等)
- 余弦平滑过渡，避免硬边界

**待完成**:
- [ ] 创建 `SplitToningPanel.jsx` UI 组件

---

### 模块 4: 自动边缘检测 (优先级: 🟡 中)

**目标**: 自动检测底片边框并裁剪

**文件结构**:
```
server/
  services/
    edge-detector.js      # 边缘检测服务
packages/shared/
  edgeDetection.js        # 边缘检测算法
```

**技术方案**:
- Canny 边缘检测
- Hough 变换检测直线
- 四边形拟合找到底片边框

**API 设计**:
```javascript
// POST /api/photos/:id/detect-edges
{
  sensitivity: 0..100,    // 检测灵敏度
  filmFormat: '35mm' | '120' | 'auto'
}

// Response
{
  success: true,
  cropRect: { x, y, w, h },
  confidence: 0.95,
  rotation: -1.5          // 建议旋转角度 (度)
}
```

**预计工时**: 2-3 天

---

### 模块 5: 后台导出队列 (优先级: 🔴 高) ✅ 已完成

**完成日期**: 2026-01-15

**实现文件**:
- `server/services/export-queue.js` - 导出队列服务
- `server/routes/export.js` - REST API 端点
- `packages/shared/filmLabExport.js` - 导出参数模块

**实现功能**:
- ExportQueue 类 (EventEmitter)
- Job 生命周期管理 (add/cancel/pause/resume)
- Worker Pool 并行处理
- WebSocket 实时进度推送
- 参数版本控制与迁移
- 参数验证和序列化

**API 端点**:
- `POST /api/export/batch` - 创建批量导出任务
- `GET /api/export/jobs` - 获取任务列表
- `DELETE /api/export/jobs/:jobId` - 取消任务
- `POST /api/export/jobs/:jobId/pause` - 暂停任务
- `POST /api/export/jobs/:jobId/resume` - 恢复任务

**待完成**:
- [ ] 创建 `ExportQueuePanel.jsx` UI 组件
- [ ] 在 RollView 添加批量导出按钮
- [ ] 集成到主路由

// WebSocket: /ws/export/:jobId
{
  type: 'progress',
  current: 12,
  total: 36,
  currentPhoto: 'IMG_001.jpg',
  status: 'processing'
}
```

**预计工时**: 2-3 天

---

## 批量导出架构设计

### 当前问题

FilmLab 目前只能在 ImageViewer 中处理单张照片。用户无法：
1. 对整卷照片批量应用相同参数
2. 批量导出处理后的照片
3. 在后台持续导出而不阻塞 UI

### 解决方案: 分层架构

```
┌─────────────────────────────────────────────────────────────┐
│                     UI Layer (客户端)                        │
├─────────────────────────────────────────────────────────────┤
│  RollView (照片列表)                                         │
│    └─ [Select All] [Apply Preset] [Batch Export]            │
│                                                              │
│  FilmLab (单张编辑)                                          │
│    └─ [Save] [Export] [Copy Settings]                       │
│                                                              │
│  ExportQueuePanel (导出队列)                                 │
│    └─ Job 1: Roll #12 - 12/36 (33%)                        │
│    └─ Job 2: Roll #15 - Queued                              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    API Layer (服务端)                        │
├─────────────────────────────────────────────────────────────┤
│  POST /api/export/batch                                      │
│    - 创建批量导出任务                                        │
│    - 返回 jobId                                              │
│                                                              │
│  GET /api/export/jobs                                        │
│    - 列出所有导出任务                                        │
│                                                              │
│  DELETE /api/export/jobs/:jobId                             │
│    - 取消/删除导出任务                                       │
│                                                              │
│  WS /ws/export/:jobId                                        │
│    - 实时进度推送                                            │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  Export Queue Service                        │
├─────────────────────────────────────────────────────────────┤
│  ExportQueue (单例)                                          │
│    ├─ addJob(options) → jobId                               │
│    ├─ cancelJob(jobId)                                      │
│    ├─ getJobStatus(jobId)                                   │
│    └─ on('progress', callback)                              │
│                                                              │
│  Worker Pool (并行处理)                                       │
│    └─ processPhoto(photoId, params, outputPath)             │
│        ├─ Load original image                               │
│        ├─ Apply filmlab-core.processPixelArray()            │
│        ├─ Apply crop/rotation                               │
│        └─ Save to outputPath                                │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│              Shared Processing Core                          │
├─────────────────────────────────────────────────────────────┤
│  packages/shared/filmlab-core.js                             │
│    └─ processPixel() / processPixelArray()                  │
│                                                              │
│  packages/shared/filmLabExport.js (新增)                     │
│    └─ buildExportParams(presetId, overrides)                │
│    └─ applyParamsToImage(imagePath, params)                 │
└─────────────────────────────────────────────────────────────┘
```

### FilmLab 接口调整

为支持批量导出，需要将 FilmLab 的处理逻辑从 UI 组件中抽离：

#### 当前架构 (耦合)
```
FilmLab.jsx
  └─ handleSave() / handleExport() / handleDownload()
      └─ 包含完整的像素处理循环
```

#### 目标架构 (解耦)
```
client/src/services/
  filmlab-export-service.js   # 导出服务封装
    └─ exportSingle(photoId, params, options)
    └─ exportBatch(photoIds, params, options)

server/services/
  filmlab-export-service.js   # 服务端导出核心
    └─ processAndExport(photoId, params, outputPath)

packages/shared/
  filmLabExport.js            # 导出参数构建器
    └─ buildExportParams(preset, overrides)
    └─ validateExportParams(params)
```

### 参数持久化

为支持"复制设置到其他照片"，需要标准化参数存储格式：

```javascript
// 照片处理参数 (存储在 photo.processing_params JSON 字段)
{
  version: 2,                    // 参数版本号
  
  // 反转
  inverted: true,
  inversionMode: 'linear',
  filmCurveEnabled: true,
  filmCurveProfile: 'portra400',
  
  // 白平衡
  red: 1.0, green: 1.0, blue: 1.0,
  temp: 0, tint: 0,
  
  // 色调
  exposure: 10, contrast: 5,
  highlights: -20, shadows: 15,
  whites: 0, blacks: 5,
  
  // 曲线
  curves: { rgb: [...], red: [...], green: [...], blue: [...] },
  
  // HSL (新增)
  hsl: {
    red: { hue: 0, saturation: 0, luminance: 0 },
    // ...其他通道
  },
  
  // 分离色调 (扩展)
  splitToning: {
    highlights: { hue: 40, saturation: 20 },
    shadows: { hue: 220, saturation: 15 },
    balance: 50
  },
  
  // 裁剪/旋转
  cropRect: { x: 0, y: 0, w: 1, h: 1 },
  rotation: 0,
  
  // 3D LUT
  lut1: 'lut_id_or_null',
  lut1Intensity: 0.8,
  lut2: null,
  lut2Intensity: 1.0
}
```

---

## 实施路线图

### 阶段 1: 基础设施 (1 周) ✅ 已完成
- [x] 创建 `server/services/export-queue.js`
- [x] 创建 `server/routes/export.js`
- [x] 实现基本的队列和 Worker Pool
- [x] 添加 WebSocket 进度推送
- [x] 创建 `packages/shared/filmLabExport.js` 参数模块
- [x] 创建 `packages/shared/filmLabHSL.js` HSL 模块
- [x] 创建 `packages/shared/filmLabSplitTone.js` 分离色调模块

### 阶段 2: 批量导出 UI (3 天) 📋 待开始
- [ ] 创建 `ExportQueuePanel.jsx`
- [ ] 在 RollView 添加批量选择和导出按钮
- [ ] 实现进度显示和任务管理

### 阶段 3: HSL 调整 UI (1 周) 📋 待开始
- [x] 实现 `filmLabHSL.js` 算法 ✅
- [ ] 创建 `HSLPanel.jsx` UI
- [ ] 集成到处理流水线
- [ ] 添加到 WebGL 着色器

### 阶段 4: RAW 支持 (1-2 周) 📋 待开始
- [ ] 评估 dcraw vs libraw 方案
- [ ] 实现 `raw-decoder.js`
- [ ] 创建 RAW 导入向导 UI
- [ ] 集成到照片上传流程

### 阶段 5: 边缘检测 (1 周) 📋 待开始
- [ ] 实现 Canny + Hough 算法
- [ ] 创建边缘检测 UI
- [ ] 集成到裁剪工具

---

## 附录

### A. 依赖包评估

| 功能 | 候选包 | 评估 |
|------|--------|------|
| RAW 解码 | dcraw (CLI), libraw (native) | dcraw 更简单，libraw 更快 |
| 任务队列 | better-queue, bull, bee-queue | better-queue 无 Redis 依赖 |
| 边缘检测 | opencv4nodejs, jimp | opencv 功能强但安装复杂 |
| WebSocket | ws, socket.io | ws 更轻量，已有使用 |

### B. 性能基准

| 操作 | 当前耗时 | 目标耗时 |
|------|----------|----------|
| 单张 HQ Export (4000px) | 2-3 秒 | < 2 秒 |
| 36 张批量导出 | N/A | < 2 分钟 |
| HSL 预览更新 | N/A | < 100ms |

---

**文档维护者**: FilmGallery 开发团队  
**最后更新**: 2026-01-15
